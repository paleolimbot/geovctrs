% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-bbox.R
\name{geo_bbox}
\alias{geo_bbox}
\alias{geo_x_range}
\alias{geo_y_range}
\alias{geo_envelope}
\alias{geo_x_envelope}
\alias{geo_y_envelope}
\title{Bounding boxes and envelopes}
\usage{
geo_bbox(x, ..., na.rm = FALSE, finite = FALSE)

geo_x_range(x, ..., na.rm = FALSE, finite = FALSE)

geo_y_range(x, ..., na.rm = FALSE, finite = FALSE)

geo_envelope(x, ..., na.rm = FALSE, finite = FALSE)

geo_x_envelope(x, ..., na.rm = FALSE, finite = FALSE)

geo_y_envelope(x, ..., na.rm = FALSE, finite = FALSE)
}
\arguments{
\item{x}{A geometry-like object, or one that can be
coerced to a geometry-like object using \code{\link[=as_geovctr]{as_geovctr()}}.}

\item{...}{Unused}

\item{na.rm}{Should NAs be removed?}

\item{finite}{Should only finite values be considered? \code{TRUE}
implies \code{na.rm = TRUE}.}
}
\value{
\code{\link[=geo_bbox]{geo_bbox()}} returns a \code{\link[=geo_rect]{geo_rect()}} of length 1,
\code{\link[=geo_envelope]{geo_envelope()}} returns a \code{\link[=geo_rect]{geo_rect()}} with the same length as \code{x},
\verb{geo_(x|y)_range()} returns a \code{\link[=geo_lim]{geo_lim()}} of length 1, and
\verb{geo_(x|y)_envelope()} returns a \code{\link[=geo_lim]{geo_lim()}} with the same length as \code{x}.
}
\description{
Whereas a \code{\link[=geo_bbox]{geo_bbox()}} of a \link[=is_geovctr]{geovctr} is always
of length 1, a \code{\link[=geo_envelope]{geo_envelope()}} shares the same length as
the vector. Both return a \code{\link[=geo_rect]{geo_rect()}}. Empty vectors
(and empty geometries) return \code{geo_rect(Inf, Inf, -Inf, -Inf)},
and \code{NA/NaN} values are removed if \code{na.rm = TRUE} (which might
mean more \code{Inf} values than you expected).
}
\examples{
geo_bbox(geo_wkt(c("POINT (30 10)", "POINT EMPTY")))
geo_envelope(geo_wkt(c("POINT (30 10)", "POINT EMPTY")))

}
